<!DOCTYPE html>
<html>
  <head>
    <title>DreamCatcher - Onboarding</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="onboarding-body">
    <% if notice %>
      <div class="alert alert-info alert-dismissible fade show mystic-alert position-fixed" style="top: 1rem; right: 1rem; z-index: 9999;" role="alert"
           data-controller="alert" data-alert-delay-value="4000">
        <%= notice %>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
    <% end %>

    <% if alert %>
      <div class="alert alert-warning alert-dismissible fade show mystic-alert position-fixed" style="top: 1rem; right: 1rem; z-index: 9999;" role="alert"
           data-controller="alert" data-alert-delay-value="5000">
        <%= alert %>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
    <% end %>

    <%= yield %>

    <!-- Modal de Noël -->
    <%= render 'shared/christmas_modal' if user_signed_in? && defined?(@show_christmas_modal) %>
  </body>
</html>
